# P1 自动建站派发验收

2026-09-18，中央建站申请到 Cloudflare Builds 的自动派发、执行和终态对账已通过真实云端验收。固定合成申请 `79d647a4-a4fb-4e8b-8403-8372c67e0452` 创建 `p1-f`；Deploy Hook URL 只保存在 Worker/Build secret 中，未写入源码、文档、数据库或日志。

普通构建 `c8f7705f-4095-4f66-85c3-e44213fbae0c` 对提交 `d0bf5d8b57108f147f0ae80329772a3a56197bca` 完成 137 个测试文件、734 项测试、P0 回归、六站 HTTPS/SSO/MCP smoke 和六站各 81 表只读核验。分组 deployment 为 `23222364-b3ee-4161-9f1e-1257494a1cf9`，version 为 `c5398061-9df2-4f1d-8fed-8c28c99a9424`，release ID 为 `f73417955ea634cbf642445c7f3277ad43bb5f24d46fcb7df254c8edf2599982`。远程 D1 代理曾多次断线，但原进程自动恢复并完成核验；没有把只读请求改成可重试写入。

前三次 Hook 尝试分别暴露提交身份交接、部署后只读请求瞬断和重叠构建争用。每次只在平台失败证据经审核后显式恢复，没有按超时推断失败或重复 POST。恢复被移至普通发布的最后一步，因此第 4 次 Hook 构建 `4d32e84d-2321-4af0-94c2-a48bb6810152` 独占构建资源，重新通过 137/734 云端测试并领取唯一请求。建站执行读取原 checkpoint 6 回执，返回 `mutations:false`；分组发布复用上述已验收 release，没有再次上传站点 Worker。

中央 D1 最终记录：派发状态 `succeeded`，尝试数 4，构建结果 `success`，提交为 `d0bf5d8b57108f147f0ae80329772a3a56197bca`，完成时间 `2026-09-18T10:38:52.386Z`，错误码为空。F 站操作仍为同一 D1 `7c9ab833-4f30-4b3c-834e-9ad032ec916d`、checkpoint 6 和原完成时间 `2026-09-18T09:16:12.146Z`。这证明 Queue/Cron/Hook、精确 build UUID 归属、失败审核恢复、幂等建站重入和构建终态对账闭环成立。

一次性验收提交随后关闭固定申请重放和失败恢复开关；禁用路径保留严格申请/恢复对象校验，并在云端记录明确的 skipped 事件。通用派发资源继续启用，后续真实申请仍走同一持久状态机。

本验收完成 P1 自动派发缺口，不代表 P2 生产队列、P3 运营观测、P4 真实站迁移、P5 扩容或完整执行计划已经完成。
