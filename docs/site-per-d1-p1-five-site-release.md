# E 建站后的五站普通发布

提交 `05cf0c427526bf4e4ec247325478aa65acec0779` 移除一次性 admission 选择，恢复普通 P0/P1 云端发布。Cloudflare 构建 `9caad704-cb31-48d8-8954-a6fd5b75c71b` 于 `2026-09-18T04:35:18.716Z` 成功结束，GitHub 检查 completed/success。718 项测试 / 133 个文件、14 项既有浏览器检查、完整角色后台与主应用构建、P0 线上回归全部通过。[构建及直接核验证据](site-per-d1-p1-five-site-validation.json)。

## 完整历史与实际发布

审定 D 文件只作为历史锚点。控制器从中央持久历史读取 C/D/E 的原计划及六步回执，得到 A/B/C/D/E 五站；没有追加手写 E 请求文件，也没有用旧 D 清单覆盖新站。分组清单摘要 `78cf3e6464ced459e96425c7ef739669f73fc98ea2d112437a0639f911a508ec` 与 E 建站后的完整清单相同。

中央 deployment `8048cd7e-5289-4195-b898-fd376bb17bed` / version `9b2803ff-ffe6-4cd1-8317-18e756c788f1`；站点 deployment `b79c8306-a5a3-4fd1-aadd-0919124a6a90` / version `217a9b0f-58a8-4593-b6de-3c9e0de4d7e8`，运行提交均为本次 `05cf0c4`。中央 v5 schema 保持 273 对象，bootstrap 新增对象为 0。

普通分组发布 `e93df4a88931fec8642ac1b5d75811a16bff6866ce47f74caa867139561806ac` 于 `04:34:50.745Z` 完成，重复执行不上传、发布回执不变。独立 API 查询确认原 C/D/E 三份建站操作与全部 18 个步骤逐字段完全不变；历史建站 deployment 保留原值，并未被普通发布改写。

## 五站验收

04:28:47Z 的真实 HTTPS/Chromium smoke 覆盖五站中央登录、选择器/SSO、host-only Cookie、原生编辑页、同 ID 20 次并发读取、隔离写入、C/D/E 分别撤权、A 生命周期与 MCP、手机布局及退出，浏览器错误为零。合成申请表单提交/重试/取消回归同样通过，未新增建站资源。

[完整逐表报告](site-per-d1-p1-five-site-report.json)从五份站点元数据和 45 段库存日志重建，包含五站各 81 表，共 405 表。逐站内容摘要和完整报告摘要均与云端输出一致；完整摘要为 `80c6f6e8076fdad4a5c40000af0a5c6359ab23e9dd7d2f43c12a4a7e0a640649`，计算对象是无末尾换行的 `JSON.stringify(report,null,2)`。核验包括两次匹配库存、SQL 外键、站点/租户/身份投影、当前部署绑定和路由。

04:36:59Z 直接查询确认五站 active、经理权限完整、生产开关关闭，路由版本 89/1/2/2/2。A 由 83 增至 89 来自既定暂停/恢复和 MCP 回归；其余登记不变。组租约为 0，无未完成发布。生产 deployment 始终为 `3046ffb1-b8ad-47ba-a373-9be5d0526c4b`，未部署到生产 Worker。

## 范围与后续

核验期间远程代理多次报告连接中断，原构建最终继续完成 D/E 检查；以平台成功终态和完整报告为准，没有因轮询或代理报错取消、重启或重放发布。

五站实际媒体对象和任务行均为 0。此报告证明当前测试数据与运行绑定一致，不是源库迁移对比、任意嵌入引用图或真实生产任务验收。自动派发的[实现约束](site-per-d1-build-dispatch.md)已核对官方接口，尚未启用。完整多组运维、共享数据投递/界面、真人交接、P1 其余项与 P2–P5 继续实施。

所有安装、测试、构建和部署由 Cloudflare Builds 执行。本地只修改源码/文档、使用 Git、检查差异和核对证据摘要；本次结果归档为文档修改，不再触发应用发布。
